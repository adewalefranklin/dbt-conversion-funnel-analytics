name: 'interview_assignment'
version: '1.0.0'
config-version: 2

profile: 'interview_assignment'

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
test-paths: ["tests"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"
